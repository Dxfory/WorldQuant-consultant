# FSRCNN models

Small OpenCV `dnn_superres` weights used by `--engine fsr`.

Source: [Saafke/FSRCNN_Tensorflow](https://github.com/Saafke/FSRCNN_Tensorflow)

Install the optional extra:

```bash
python -m pip install -e ".[sr]"
```
